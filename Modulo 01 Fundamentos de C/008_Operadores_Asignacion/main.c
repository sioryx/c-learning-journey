#include <stdio.h>

int main(void)
{
    // Declaración e inicialización de la variable
    int vidas = 5;

    // Restar 2 al valor de la variable utilizando
    // el operador de asignación compuesta
    vidas -= 2;

    // Mostrar el resultado
    printf("Quedan: %d\n", vidas);

    // Finaliza correctamente el programa
    return 0;
}